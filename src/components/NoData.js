import Typography from "@mui/material/Typography";

const NoData = () => (
  <Typography
    variant="h5"
    align="center"
    sx={{ py: 7, color: "text.disabled", opacity: 0.5 }}
  >
    No data
  </Typography>
);

export default NoData;
